/*
var chartData = {
  series: [parseInt(Cent), parseInt(Lib), parseInt(Mod), parseInt(Kri), parseInt(Soc), parseInt(Van), parseInt(Mil), parseInt(Sd)]
};

var sum = function(a, b) { return a + b };

new Chartist.Pie('.ct-chart', chartData, {
  labelInterpolationFnc: function(value) {
    return Math.round(value / chartData.series.reduce(sum) * 100) + '%';
  }
});


new Chartist.Pie2('.ct-chart2', {
  series: chartData
}, {
  donut: true,
  donutWidth: 60,
  startAngle: 270,
  total: 200,
  showLabel: false
});
*/
